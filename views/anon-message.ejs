<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anonymous Message - Neer's Journey</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        /* Reuse styles from login.ejs */
        :root {
            --primary-blue: #007bff;
            --primary-blue-light: #66b2ff;
            --primary-blue-dark: #0056b3;
            --primary-white: #ffffff;
            --light-grey: #f0f4f8;
            --text-color: #495057;
            --border-color: #e2e6eb;
            --box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            --transition-speed: 0.4s;
            --border-radius-lg: 15px;
            --border-radius-md: 10px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-grey);
            color: var(--text-color);
            line-height: 1.7;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 25px;
        }

        .section-box {
            padding: 60px 0;
            background-color: var(--primary-white);
            border-radius: var(--border-radius-lg);
            margin: 20px 0;
            box-shadow: var(--box-shadow);
        }

        h2 {
            font-family: 'Montserrat', sans-serif;
            font-size: 2.5rem;
            color: var(--primary-blue-dark);
            margin-bottom: 20px;
            text-align: center;
        }

        .message-form-container {
            max-width: 500px;
            margin: 0 auto;
            text-align: center;
        }

        .message-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .message-form textarea {
            padding: 12px;
            border: 1px solid var(--border-color);
            border-radius: var(--border-radius-md);
            font-size: 1rem;
            min-height: 150px;
        }

        .btn-submit {
            background: linear-gradient(90deg, var(--primary-blue), var(--primary-blue-light));
            color: var(--primary-white);
            padding: 15px;
            border: none;
            border-radius: var(--border-radius-md);
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            box-shadow: var(--box-shadow);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0, 123, 255, 0.4);
        }

        .success-message {
            color: green;
            text-align: center;
            margin-bottom: 20px;
        }

        .error-message {
            color: red;
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <section id="anon-message" class="section-box">
        <div class="container">
            <h2><i class="fas fa-envelope section-icon"></i> Send Anonymous Message</h2>
            <div class="message-form-container">
                <% if (success) { %>
                    <p class="success-message"><%= success %></p>
                <% } %>
                <% if (error) { %>
                    <p class="error-message"><%= error %></p>
                <% } %>
                <form action="/send-message" method="POST" class="message-form">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <textarea name="message" placeholder="Your message..." required></textarea>
                    <button type="submit" class="btn-submit">Send</button>
                </form>
                <p><a href="/">Back to Home</a></p>
            </div>
        </div>
    </section>
</body>
</html>