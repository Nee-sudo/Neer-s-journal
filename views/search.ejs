<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Reuse styles from login.ejs and home.ejs */
        body { font-family: 'Poppins', sans-serif; }
        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        h2 { font-family: 'Montserrat', sans-serif; color: #0056b3; }
        .box-container { display: flex; gap: 20px; overflow-x: auto; }
        .box { flex: 0 0 300px; background: #fff; padding: 20px; border-radius: 10px; box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08); }
        @media (max-width: 768px) { .box-container { flex-direction: column; } .box { flex: 0 0 auto; width: 100%; } }
    </style>
</head>
<body>
    <div class="container">
        <h2>Search Results for "<%= query %>"</h2>
        <h3>Journals</h3>
        <div class="box-container">
            <% entries.forEach(entry => { %>
                <div class="box">
                    <h3><%= entry.title %></h3>
                    <div><%= entry.content %></div>
                </div>
            <% }) %>
        </div>
        <h3>Friends</h3>
        <div class="box-container">
            <% friends.forEach(friend => { %>
                <div class="box">
                    <h3><%= friend.name %></h3>
                    <p><%= friend.jobCategory %>, <%= friend.country %></p>
                </div>
            <% }) %>
        </div>
        <a href="/home">Back to Home</a>
    </div>
</body>
</html>